[project]
name = "resumix-server"
version = "0.3.2"
description = "resumix HTTP API: turn a job description into tailored CV data and a compiled LaTeX PDF."
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "resumix-contracts",
    # Web layer. uvicorn plain, not [standard]: no uvloop/httptools/websockets.
    "fastapi>=0.115",
    "uvicorn>=0.30",
    # Required for File()/Form() parts — FastAPI raises without it.
    "python-multipart>=0.0.9",
    # Any OpenAI-compatible endpoint (see resources/models.toml).
    "openai",
    # Templating the LaTeX CV.
    "Jinja2",
    # Page counting on the compiled PDF.
    "pypdf",
    # .env secrets for local runs.
    "python-dotenv",
]

[project.scripts]
resumix-api = "resumix_server.server:main"

[tool.uv.sources]
resumix-contracts = { workspace = true }

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

# resources/ lives next to src/, not inside it, so it needs its own
# package-dir entry and an explicit packages list (packages.find only scans
# "where").
[tool.setuptools]
package-dir = { "" = "src", "resumix_server.resources" = "resources" }
packages = [
    "resumix_server",
    "resumix_server.api",
    "resumix_server.api.routers",
    "resumix_server.pipeline",
    "resumix_server.resources",
]

[tool.setuptools.package-data]
"resumix_server.resources" = ["*.txt", "*.toml", "*.jinja", "*.png"]
